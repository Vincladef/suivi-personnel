[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[[scheduled.functions]]
  name = "send-goal-reminders"
  cron = "0 8 * * *"
